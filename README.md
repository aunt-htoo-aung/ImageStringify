# ImageStringify
Image Stringify is a simple, lightweight web application built with JSP and Servlet technology that enables users to convert images into Base64 encoded data URLs. This tool is designed to help developers and web designers easily embed images directly into HTML, CSS, or JSON without the need for separate image files.
